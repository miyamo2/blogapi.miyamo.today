name: ci_core_graphql

on:
  push:
    branches:
      - "feat/**"
      - "refactor/**"
      - "bugfix/**"
      - "hotfix/**"
    paths:
      - "core/graphql/**"
  pull_request:
    branches:
      - "main"
      - "develop"
    paths:
      - "core/graphql/**"

permissions: write-all

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/custom_actions/ci/
        with:
          target: core/graphql
