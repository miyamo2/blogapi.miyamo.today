name: ci_core_grpc

on:
  push:
    branches:
      - "feat/**"
      - "refactor/**"
      - "bugfix/**"
      - "hotfix/**"
    paths:
      - "core/grpc/**"
  pull_request:
    branches:
      - "main"
      - "develop"
    paths:
      - "core/grpc/**"

permissions: write-all

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/custom_actions/ci/
        with:
          target: core/grpc
          upload_codecov: true

